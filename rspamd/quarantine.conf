# Rspamd quarantine module configuration
# Quarantines emails exceeding the spam score threshold
# Note: quarantine requires redis and proper module loading
quarantine {
  redis_key = "bloom_quarantine"
  messages_key = "quarantine_messages"
  max_size = 1M
  max_num_of_messages = 1000
  max_duration = 2d
}
